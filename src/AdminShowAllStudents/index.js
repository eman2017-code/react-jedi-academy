import React, { Component } from "react";
import { Form } from "semantic-ui-react";

class AdminShowAllStudents extends Component {
	constructor(props) {
		super()

		this.state = {};
		// sets padawans in state 
		padawans: [],
		// set courses in state 
		courses: []
	}

	render() {
		return(
			// use function instead? 
			// if padawans are enrolled in courses, display in in component 
			// const enrolledStudents = props.padawans.map((padawan) => {
				// return
			// })

			// get the students that are registered (enrolled) 
			// loop through all of the students
			// display them on the screen


			)
	}
}



Export default AdminShowAllStudents 